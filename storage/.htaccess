# Deny all web access to storage directory
# This prevents attackers from accessing log files via browser

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
